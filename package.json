{
  "name": "droom-it",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check api/start-project.js && node --check script.js && node --check middleware.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2",
    "@vercel/functions": "^3.7.5",
    "nodemailer": "^8.0.10"
  }
}
